/**
 * Unit conversions, kept in one place so they cannot drift.
 *
 * These are not tuning: the thresholds and cadences built out of them live in
 * `../config.ts`. This file only says how long a second is.
 */

export const MS_PER_SECOND = 1000;

export const MS_PER_HOUR = 3_600_000;
